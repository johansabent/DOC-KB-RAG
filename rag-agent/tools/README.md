## Tools

This folder holds one-off helper scripts used to inspect Gemini model support
and embedding dimensions. The runtime entrypoints for the RAG pipeline remain
at `rag-agent/ingest.py` and `rag-agent/query.py`.
